feed_meta:
  unique_identifier: "valid_pipeline_feed"
  vacuum_hours: 168
  pipeline: "nightly_ingest"

load_details:
  type: "FULL_LOAD"

source:
  type: "hive_metastore"
  format: "delta"
  schema: "demo"
  table: "employee"

target:
  type: "hive_metastore"
  format: "delta"
  schema: "demo"
  table: "employee_target"

feed_specs:
  primary_key: "id"
  custom_selection:
    enabled: false

data_quality_checks:
  - check_group_identifier: "group_pipeline"
    description: "Check with a valid pipeline"
    run_type: "PRE_LOAD"
    dependency_datasets:
    column: "event_ts"
    checks:
      - name: "null_check"
